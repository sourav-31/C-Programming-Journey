#include <stdio.h>
int main(){
    int i = 3;
    do
    {
        printf("%d\n",i);
        i++;
    } while (i>=5); // because i is not > than 5 but then also line will execute once
    
return 0;
}
//over