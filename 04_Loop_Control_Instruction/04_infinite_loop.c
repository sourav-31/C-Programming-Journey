#include <stdio.h>
int main(){
    int i =0;
    while(2<10){ //condition which is always true
        printf("The value of i is %d\n",i);
        i++;
    }
return 0;
// for infinite loop we can make condition true for every alteration or we can take same i
}