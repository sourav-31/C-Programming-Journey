#include <stdio.h>
int main(){
    int a=1, b=0;
    printf("The value of a and b is %d\n",a&&b);
    printf("The value of a or b is %d\n",a||b);
    printf("The value of not(a) is %d",!a); //Value of a will swap : if it is 0, it become 1 & if it is 1, it become 0



return 0;
}