// Write a program to check whether a number is divisible by 97 or not.

#include <stdio.h>
int main()
{
    int a = 256164;
    printf("The value of a%97 is : %d",a%97);
    return 0;
    // Since value of a%97 is not 0 therefore a is not divisible by 97
}
